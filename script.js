jQuery(document).ready(function($) {

    var Spy = $('.menu').anchorSpy({
            margin: 100,
            speed: 1000,
            next: '.next',
            prev: '.prev',
            active_class: 'active',
            active_parent: true
        });


});